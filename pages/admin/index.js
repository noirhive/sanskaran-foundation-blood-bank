import Layout from '../../components/Layout';

export default function Admin() {
  return (
    <Layout>
      <h2 className="text-2xl mb-4">Admin Panel (placeholder)</h2>
      <p>This panel allows viewing reveal requests, audits, and managing donors. Use the AdminPanel in request flow for now.</p>
    </Layout>
  )
}
